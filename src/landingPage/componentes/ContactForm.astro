---
import CitiesSelect from "../componentes/ContactForm/CitiesSelect";
---

<section
  id="contactForm"
  class="py-10 sm:py-16 md:py-20 bg-neutral-950 flex items-center justify-center px-4 sm:px-6 lg:px-8"
>
  <div
    class="bg-transparent rounded-lg px-4 sm:px-6 md:px-8 w-full max-w-sm sm:max-w-md md:max-w-lg"
  >
    <div class="mb-6 sm:mb-8 text-center">
      <h1 class="text-2xl sm:text-3xl font-extrabold text-white">¡Hablemos!</h1>
      <p class="mt-2 text-sm sm:text-base text-white">
        Envía tus datos para que nos pongamos en contacto y recibas información
        detallada.
      </p>
    </div>
    <form
      action="/api/v1/submit-contact"
      method="POST"
      class="space-y-4 sm:space-y-6"
    >
      <!-- Primera fila: Nombre y Apellido -->
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
        <div>
          <label for="nombre" class="block text-sm font-medium text-white"
            >Nombre</label
          >
          <input
            name="name"
            type="text"
            placeholder="Nombre"
            class="mt-1 p-3 sm:p-4 pe-9 block w-full border-gray-200 rounded-lg text-sm focus:border-blue-500 focus:ring-blue-500 disabled:opacity-50 disabled:pointer-events-none dark:bg-neutral-900 dark:border-neutral-700 dark:text-neutral-400 dark:focus:ring-neutral-600"
          />
        </div>
        <div>
          <label for="apellido" class="block text-sm font-medium text-white"
            >Apellido</label
          >
          <input
            name="lastname"
            type="text"
            placeholder="Apellido"
            class="mt-1 p-3 sm:p-4 pe-9 block w-full border-gray-200 rounded-lg text-sm focus:border-blue-500 focus:ring-blue-500 disabled:opacity-50 disabled:pointer-events-none dark:bg-neutral-900 dark:border-neutral-700 dark:text-neutral-400 dark:focus:ring-neutral-600"
          />
        </div>
      </div>

      <!-- Segunda fila: Estado y Ciudad -->
      <CitiesSelect client:load />

      <!-- Tercera fila: Paquete y Modalidad -->
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
        <div>
          <label for="plan" class="block text-sm font-medium text-white"
            >Paquete</label
          >
          <select
            name="plan"
            class="mt-1 p-3 sm:p-4 pe-9 block w-full border-gray-200 rounded-lg text-sm focus:border-blue-500 focus:ring-blue-500 disabled:opacity-50 disabled:pointer-events-none dark:bg-neutral-900 dark:border-neutral-700 dark:text-neutral-400 dark:focus:ring-neutral-600"
          >
            <option>Básico</option>
            <option>Premium</option>
            <option>Pro</option>
          </select>
        </div>
        <div>
          <label for="mode" class="block text-sm font-medium text-white"
            >Modalidad</label
          >
          <select
            name="mode"
            class="mt-1 p-3 sm:p-4 pe-9 block w-full border-gray-200 rounded-lg text-sm focus:border-blue-500 focus:ring-blue-500 disabled:opacity-50 disabled:pointer-events-none dark:bg-neutral-900 dark:border-neutral-700 dark:text-neutral-400 dark:focus:ring-neutral-600"
          >
            <option>Presencial</option>
            <option>Virtual</option>
          </select>
        </div>
      </div>

      <!-- Método de Contacto -->
      <div>
        <label for="contact_type" class="block text-sm font-medium text-white"
          >Método de Contacto</label
        >
        <select
          name="contact_type"
          class="mt-1 p-3 sm:p-4 pe-9 block w-full border-gray-200 rounded-lg text-sm focus:border-blue-500 focus:ring-blue-500 disabled:opacity-50 disabled:pointer-events-none dark:bg-neutral-900 dark:border-neutral-700 dark:text-neutral-400 dark:focus:ring-neutral-600"
        >
          <option>Correo Electrónico</option>
          <option>Teléfono</option>
          <option>WhatsApp</option>
        </select>
      </div>

      <!-- Información de Contacto -->
      <div>
        <label for="contact_info" class="block text-sm font-medium text-white"
          >Información de Contacto</label
        >
        <input
          name="contact_info"
          type="text"
          placeholder="Información de Contacto"
          class="mt-1 p-3 sm:p-4 pe-9 block w-full border-gray-200 rounded-lg text-sm focus:border-blue-500 focus:ring-blue-500 disabled:opacity-50 disabled:pointer-events-none dark:bg-neutral-900 dark:border-neutral-700 dark:text-neutral-400 dark:focus:ring-neutral-600"
        />
      </div>

      <!-- Botón de Aceptar -->
      <div>
        <button
          type="submit"
          class="w-full py-2.5 sm:py-3 px-4 bg-primary text-gray-300 text-sm sm:text-base font-semibold rounded-md shadow hover:bg-gray-600 transition-colors focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500"
        >
          Aceptar
        </button>
      </div>
    </form>
  </div>
</section>
